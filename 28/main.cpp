#include <bits/stdc++.h>

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);

    double N;
    std::cin >> N;

    double r;
    r = sqrt(N);
    printf("%.2f\n", r);

    return 0;
}
